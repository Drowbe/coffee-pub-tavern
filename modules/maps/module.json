{
  "id": "maps",
  "name": "Maps",
  "version": "0.6.0",
  "icon": "map-location-dot",
  "description": "A map of every place your room has: the places you have saved and the items other modules put somewhere. Pick a spot to save a new place.",
  "author": "Coffee Pub",
  "scope": [
    "room"
  ],
  "surfaces": {
    "page": {
      "entry": "page.html"
    },
    "panel": {
      "entry": "panel.html",
      "width": 520,
      "height": 600,
      "mode": [
        "dock",
        "float"
      ]
    }
  },
  "permissions": [
    {
      "key": "view",
      "label": "See the map and its places",
      "default": {
        "user": true,
        "guest": true,
        "moderator": true
      }
    },
    {
      "key": "edit",
      "label": "Save places from the map and put items on it",
      "default": {
        "user": true,
        "guest": false,
        "moderator": true
      }
    }
  ],
  "settings": [
    {
      "key": "map",
      "label": "Map file",
      "type": "file",
      "folder": "map-tiles",
      "scope": "server",
      "default": "",
      "help": "A PMTiles map file (a region or the world) in the map-tiles folder inside the Maps module folder (modules/maps/map-tiles/ in the data folder). Nothing shows until one is chosen."
    }
  ],
  "access": {
    "read": "view",
    "write": "edit"
  },
  "actions": {
    "provides": [
      {
        "name": "showOnMap",
        "label": "Show on the map",
        "local": true,
        "input": {
          "ref": "ref"
        }
      }
    ],
    "uses": [
      "*"
    ]
  },
  "refs": {
    "consumes": [
      "*"
    ]
  },
  "requires": [
    "places"
  ]
}
