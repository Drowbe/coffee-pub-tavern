// The page a turned-off or replaced personal link lands on (GET /j/:token): the environment's name and icon.
// A file of its own, not an inline script: the page's content security policy allows scripts from this site only.
import { loadBranding } from '/brand.js';

loadBranding();
