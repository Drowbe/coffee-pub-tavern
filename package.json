{
  "name": "coffee-pub-tavern",
  "version": "0.2.0",
  "description": "Self-hosted voice and video for the Coffee Pub table, with per-player OBS views.",
  "private": true,
  "license": "MIT",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "node --check public/gate-worklet.js && node --check server/index.js && node --check server/store.js && node --check server/auth.js && node --check public/room.js && node --check public/view.js && node --check public/admin.js && node --check public/login.js && node --check public/profile.js && node --check public/brand.js && node --check public/roomconfig.js"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "@livekit/track-processors": "^0.8.0",
    "express": "^5.2.1",
    "livekit-client": "^2.22.3",
    "livekit-server-sdk": "^2.19.0"
  }
}
